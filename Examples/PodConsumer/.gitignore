# Xcode project is xcodegen-generated — regenerate via `xcodegen` in this folder.
PodConsumer.xcodeproj/

# CocoaPods artifacts — regenerated by `pod install`.
PodConsumer.xcworkspace/
Podfile.lock
Pods/

# Per-user state.
*.xcuserdata/
