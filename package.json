{
  "private": true,
  "description": "Dev tooling for beeping-ios (commitlint + lefthook only — no runtime JS)",
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@beeping.io/commitlint-config": "^0.1.0"
  }
}
